import React, { useState, useEffect } from "react";
import Spinner from "./Spinner";

const MasonaryLayout = () => {
  const [loading, setLoading] = useState(false);

  return <div>MasonaryLayout</div>;
};

export default MasonaryLayout;
